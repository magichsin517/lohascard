# 樂活卡卡 — 2026-04-17 夜間工程交接

> 你睡著那段時間(2026-04-17 晚 ~ 04-18 早)做的事。

## 早上你只需要做這兩件事

### 1. 部署到 Vercel(必做)

打開終端機,跑:

```bash
cd "/Users/yunghsin/Documents/文稿 - 王永興的iMac/working/2026/lohascard"
npx vercel --prod
```

大約 1~2 分鐘會完成,上線網址一樣是 <https://lohascard.vercel.app>。

### 2. 確認上線結果(10 秒)

打開 <https://lohascard.vercel.app> 看:
- Hero 應該寫「For 60+ · Taiwan」和「我們幫你從全台里長、樂齡中心...」
- 篩選區有三排:**類別 → 費用 → 縣市**
- 活動清單應該有 220+ 筆,分佈全台 22 縣市
- 點任一張卡 → 詳細頁 → 最下面會有「原始公告(含詳細課表與聯絡方式)」連結,可點到教育部樂齡網

## 本次異動清單

### 資料庫(Supabase)

- 新增 210 筆活動,來源「教育部樂齡學習網」,橫跨全台 22 縣市,每縣市各 10 筆
- 既有 8 筆示範資料的 `tags` 補上定價標籤(之前只有 id=6 少「收費」)
- 總筆數:228

### 前端

- `lib/supabase.ts` — 新增 `PricingTier` 型別、`PRICING_TIERS` / `PRICING_STYLE` 常數、`pricingTierOf()` 工具
- `lib/taiwan-regions.ts` — **新檔** — 全台 22 縣市 + 所轄鄉鎮市區(368 個行政區)清單
- `components/PricingFilter.tsx` — **新檔** — 費用篩選(免費/小額收費/收費)
- `components/DistrictFilter.tsx` — 改寫,從雙北 20 區擴成全台 368 區,用兩層選單(縣市 → 區)
- `components/ActivityCard.tsx` — 卡片徽章加費用徽章(綠/黃/橘)
- `app/page.tsx` — Hero 文案、footer 資料來源、加 `PricingFilter`、加 `?pricing=` 和 `?city=` 查詢參數
- `app/activities/[id]/page.tsx` — `SignupCard` 加「原始公告連結」顯示,處理只有 source_url 沒電話的情況

### 爬蟲

- `scripts/scrape/senioredu_moe.py` — **新檔** — 教育部樂齡網爬蟲(220 行 Python,無第三方套件)
- `scripts/scrape/README.md` — 使用說明

## 資料品質統計

共 228 筆:
- 各縣市覆蓋:22/22(台北 18 筆,其他每縣市各 10 筆)
- 分類命中:learning(97%),其他(3%)
- district 命中:90% (206/228)
- 定價標籤命中:100%(226 免費,1 小額,1 收費)

**誠實告知:**爬到的 220 筆絕大多數是「樂齡中心當月課程表」這種層級的公告,不是單一活動。用戶點進去會看到原始公告(裡面通常是 PDF 或圖片版的當月課表)。要抓到單堂課的細節(時段、老師、地點)要另外解析 PDF,下一輪做。

## 需要注意的事

**本次未做 GitHub 自動部署串接。** 以後每次改 code 還是要手動 `npx vercel --prod`。

**service_role key 流出風險。** 我只放在沙盒 `/tmp/` 的環境變數,沒寫入 repo 也沒存到 memory。但你自己給過一次,提醒:那把 key 有完整寫入權限,如果哪天懷疑外洩,到 Supabase Dashboard → Project Settings → API → `Reset service_role key`。

**爬蟲擴充下一步建議(非急迫):**
1. 分頁抓更多歷史公告(每縣市從 10 筆擴到 30-50 筆)
2. 加解析 ContentGoverment 的縣市政府公告(樂齡專案、旅遊、健檢)
3. 衛福部社區照顧關懷據點(3000+ 據點,venue 資料豐富)

## 回滾方式(如果上線後發現有嚴重問題)

**回滾程式碼:** git log 看這次 commit(如果你有 commit),`git revert` 回去。沒 commit 的話,改回以下 4 個檔的最近版:
- `app/page.tsx`
- `components/DistrictFilter.tsx`
- `components/ActivityCard.tsx`
- `lib/supabase.ts`

**回滾資料:** 刪掉新爬進來的 220 筆:

```sql
-- 在 Supabase SQL Editor 跑
delete from activities where source_name = '教育部樂齡學習網';
```
